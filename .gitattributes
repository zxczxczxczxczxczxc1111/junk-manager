# Переводы строк нормализуются в репозитории и разворачиваются в CRLF при
# выкладке. Без этого файла три слияния подряд дали конфликт на ВЕСЬ
# scripts/mutate.ps1: ветки хранили один и тот же файл в CRLF и в LF, и git
# честно считал разными все 480 строк. Содержательных расхождений там было две
# строки.
* text=auto

# Двоичное не трогать.
*.png binary
*.ico binary

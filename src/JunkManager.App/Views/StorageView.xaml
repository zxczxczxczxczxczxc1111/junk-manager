<UserControl x:Class="JunkManager.App.Views.StorageView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:c="clr-namespace:JunkManager.App.Controls"
 xmlns:conv="clr-namespace:JunkManager.App.Converters" x:ClassModifier="internal">
 <UserControl.Resources><conv:ByteSizeConverter x:Key="Size"/></UserControl.Resources>
 <!-- One list, recycled rows, and no fake total that adds a folder to its own children. -->
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <StackPanel>
   <c:TrackedText Text="МЕСТО НА ДИСКЕ" Tracking="{StaticResource TrackingEyebrow}" FontFamily="{StaticResource TextFont}" FontSize="{StaticResource FontSizeEyebrow}" Foreground="{StaticResource Ink3Brush}"/>
   <TextBlock Text="Что занимает место" FontFamily="{StaticResource DisplayFont}" FontSize="{StaticResource FontSizeH1}" Foreground="{StaticResource InkBrush}" Margin="0,6,0,8" AutomationProperties.AutomationId="storage-heading"/>
   <TextBlock Text="Размер папки не равен мусору. Вложенные папки и общие файлы могут повторяться в строках, поэтому складывать эти числа нельзя." TextWrapping="Wrap" Foreground="{StaticResource Ink2Brush}"/>
  </StackPanel>
  <Grid Grid.Row="1" Margin="0,18,0,16">
   <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Поиск по пути или группе" AutomationProperties.AutomationId="storage-search" Margin="0,0,16,0"/>
   <CheckBox Grid.Column="1" Content="Найти 25 крупных файлов на диске" IsChecked="{Binding DeepScan}" VerticalAlignment="Center" Margin="0,0,16,0" AutomationProperties.AutomationId="storage-deep"/>
   <Button Grid.Column="2" Content="Сохранить CSV" Command="{Binding ExportCommand}" Margin="0,0,12,0" AutomationProperties.AutomationId="storage-export"/>
   <Button Grid.Column="3" Content="Посчитать" Style="{StaticResource PrimaryButton}" Command="{Binding AnalyzeCommand}" AutomationProperties.AutomationId="storage-analyze"/>
  </Grid>
  <ListBox x:Name="StorageRows" Grid.Row="2" ItemsSource="{Binding VisibleRows}" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch"
   ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.AutomationId="storage-rows">
   <ListBox.ItemTemplate><DataTemplate>
    <Border Padding="14,12" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1">
     <Grid>
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="140"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel>
       <TextBlock Text="{Binding Group}" Foreground="{StaticResource Ink3Brush}" FontSize="{StaticResource FontSizeMeta}"/>
       <TextBlock Text="{Binding Path}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" Foreground="{StaticResource InkBrush}" Margin="0,4,0,4"/>
       <TextBlock Text="{Binding Note}" TextWrapping="Wrap" Foreground="{StaticResource Ink2Brush}" FontSize="{StaticResource FontSizeMeta}"/>
      </StackPanel>
      <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="12,0,16,0">
       <TextBlock Text="{Binding Bytes, Converter={StaticResource Size}}" FontFamily="{StaticResource MonoFont}" Foreground="{StaticResource InkBrush}" HorizontalAlignment="Right"/>
       <TextBlock Text="неполный размер" FontSize="{StaticResource FontSizeMeta}" Foreground="{StaticResource Ink3Brush}">
        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Partial}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
       </TextBlock>
      </StackPanel>
      <Button Grid.Column="2" Content="Открыть папку" Style="{StaticResource SmallButton}" VerticalAlignment="Center" Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" AutomationProperties.AutomationId="storage-open"/>
     </Grid>
    </Border>
   </DataTemplate></ListBox.ItemTemplate>
  </ListBox>
  <TextBlock Grid.Row="2" Text="{Binding EmptyMessage}" TextWrapping="Wrap" MaxWidth="440" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Foreground="{StaticResource Ink2Brush}" AutomationProperties.AutomationId="storage-empty">
   <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Items.Count, ElementName=StorageRows}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
  </TextBlock>
  <TextBlock Grid.Row="3" Text="{Binding Status}" Foreground="{StaticResource Ink2Brush}" TextWrapping="Wrap" Margin="0,14,0,0" AutomationProperties.AutomationId="storage-status"/>
 </Grid>
</UserControl>

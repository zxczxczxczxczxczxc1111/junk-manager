<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!--
      Внутреннее видно набору тестов. Открыт SafetyGuard.IsAtOrUnder: приёмка
      посева обязана считать находку про каталог засчитанной для файла внутри
      него, а правило вложенности должно быть ОДНО на весь продукт. Своя копия
      этого сравнения в тестах проверяла бы копию, а не guard.
      Публичным делать нельзя: наружу это не API продукта.
    -->
    <InternalsVisibleTo Include="JunkManager.Tests" />
  </ItemGroup>

</Project>
